import '../globals.css';

export const metadata = { title: 'Inloggen', robots: { index: false, follow: false } };

export default function InloggenLayout({ children }: { children: React.ReactNode }) {
  return children;
}
